# Healthcare Registration Portal

A simple healthcare registration portal built with Java Spring Boot.

## Getting Started

1. Ensure you have Java 17+ and Maven installed.
2. From the project root, run:
   ```bash
   mvn spring-boot:run
   ```
3. Open `http://localhost:8080/register` in your browser to access the registration form.
